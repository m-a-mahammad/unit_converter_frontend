# 🔄 تطبيق ويب لتحويل الوحدات (Unit Converter Web App)

تطبيق ويب بسيط لتحويل الوحدات بين أنظمة مختلفة (**الطول**، **الوزن**، **الحرارة**)، بواجهة تفاعلية تعتمد على **JavaScript** و**HTML** و**CSS**. يتم إرسال بيانات التحويل إلى السيرفر عبر واجهة `fetch` القياسية.

---
## 🚀 الميزات الأساسية

- **📏 تحويل وحدات الطول** (cm, m, km, in, ft, yd, mi...).
- **⚖️ تحويل وحدات الوزن** (g, kg, lb, oz...).
- **🌡️ تحويل درجات الحرارة** (°C, °F, K).
- **🧭 واجهة Tabs** لتبديل نوع التحويل بسهولة.
- **📤 إرسال البيانات إلى السيرفر** باستخدام `fetch` و `URLSearchParams`.
- **🎯 عرض النتيجة** بشكل ديناميكي داخل الصفحة.
---

## 🛠️ المتطلبات التقنية

- متصفح حديث يدعم **ES Modules**.
- سيرفر **Node.js** أو أي سيرفر آخر يستقبل طلبات `POST` على المسار **`/submit`** ويعيد نتيجة التحويل بصيغة **JSON**.

---
## 📦 التثبيت والتشغيل

1. **وضع الملفات:** ضع الملفات التالية في مجلد المشروع:
- `index.html`
- `style.css`
- `script.js`
- `constants.js`
- `add-tab-contents.util.js`

1. **تشغيل السيرفر:** شغّل السيرفر المحلي على العنوان `http://localhost:3000`.

---
## ▶️ دليل الاستخدام

### 🧪 التبويبات (Tabs)

اضغط على التبويب المطلوب لتبديل نوع التحويل:
- **Length (الطول):** لعرض خيارات وحدات الطول.
- **Weight (الوزن):** لعرض خيارات وحدات الوزن.
- **Temperature (الحرارة):** لعرض خيارات وحدات الحرارة.
### 📤 إرسال البيانات والتحويل

1. أدخل **القيمة** المراد تحويلها.
2. اختر **الوحدة الأصلية** والوحدة **الهدف**.
3. اضغط على زر **"Convert"**.
4. يتم إرسال الطلب إلى المسار **`/submit`**، ويُعرض الناتج داخل العنصر `<div class="conversion_result">`.
---
## 📂 هيكل المشروع

```
├── index.html                    # الصفحة الرئيسية والواجهة
├── style.css                     # تنسيق الواجهة
├── script.js                     # المنطق التفاعلي (معالجة الأحداث، إرسال fetch)
├── constants.js                  # تعريف خيارات الوحدات المختلفة
├── add-tab-contents.util.js      # وظيفة مساعدة لعرض محتوى التبويبات
└── README.md                     # ملف التوثيق هذا
```

---

## 📄 تنسيق الاستجابة من السيرفر

يجب أن تكون استجابة السيرفر بصيغة **JSON**، مثال:

```json
{
  "result": 609,
  "resultUnit": "cm",
  "lengthUnit": "ft" 
  // ملاحظة: قد يختلف مفتاح الوحدة الأصلية (مثل lengthUnit) حسب نوع التحويل
}
```

---

## 🧠 ملاحظات تقنية إضافية

- يُستخدم الحدث `DOMContentLoaded` لضبط التبويب النشط الأولي بناءً على قيمة `window.location.hash`.
- يتم إرسال البيانات عبر `fetch` باستخدام نوع المحتوى **`application/x-www-form-urlencoded`**.
- يتم عرض النتيجة النهائية داخل عنصر ذي الفئة `<h2 class="calc_result">`.
